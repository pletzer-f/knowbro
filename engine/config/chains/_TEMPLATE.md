<!-- version: 0 -->
<!-- TEMPLATE — copy to NN-name.md to add a new inference chain. Files in this folder
     are loaded in filename order and concatenated into the engine's system prompt.
     Delete this comment block in real chains is not required; HTML comments are kept
     out of the prompt by the loader. -->
# Inference chain: <name>

**Goal:** <one sentence: what unobservable thing this chain estimates and why it matters>

**Method:**
1. <inputs to look for, in priority order>
2. <reasoning steps, with concrete anchors/benchmarks where possible>
3. <how to produce the output — prefer ranges>

**Cross-checks:** <independent second methods; what convergence/divergence means>

**Output:** <what the chain must produce: estimate + inference path + confidence + deal implication>

**What raises confidence:** <the specific data that would move low → medium → high>
